# Portfolio

Mijn site. PHP en MySQL, geen framework.

```
php -S localhost:8000
```

Database:

```
mysql -u root < database/schema.sql
```

Config: `includes/config.php`.

Online via Render: New Web Service, deze repo, Docker, Free.
